# Python
__pycache__/
*.py[cod]
*.egg-info/
*.egg
.eggs/
build/
dist/
.venv/
venv/
env/

# Pytest / Tooling
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/

# IDE / OS
.vscode/
.idea/
.DS_Store

# Env files (keep .env.example)
.env
.env.local
.env.*.local
docker/.env.prod

# Node (for Playwright e2e)
node_modules/
tests/e2e/test-results/
tests/e2e/playwright-report/
tests/e2e/.playwright/

# MATLAB legacy reference (kept locally, not in repo).
# Out of scope per project plan; ~519 MB including LEDA studies.
# scp from appsdev2 to refresh:
#   scp -r appsdev2:/home/als/physbase/users/thellert/automated_startup legacy/
legacy/
.superpowers/

# Claude Code per-machine settings (keep .claude/settings.json — project-wide)
.claude/settings.local.json
